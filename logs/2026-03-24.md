# 📖 2026-03-24 学习日志

**日期:** 2026 年 3 月 24 日 星期二  
**学习时段:** 13:00 开始  
**计划时长:** 90 分钟

---

## 🎯 今日目标

- [ ] C++ 函数深入（参数传递、重载、模板函数）
- [ ] C++ 面向对象基础（类、对象、继承）
- [ ] 实践练习：实现一个简单的类
- [ ] GitHub 学习记录同步 ✅

---

## 📚 学习内容

### C++ 函数深入

**参数传递方式:**
```cpp
// 值传递（复制）
void byValue(int x) { x = 100; }  // 不影响原值

// 引用传递（C++11 推荐）
void byReference(int& x) { x = 100; }  // 影响原值

// 常量引用（避免复制，不修改）
void byConstRef(const std::string& str) { 
    // 不能修改 str
}

// 指针传递
void byPointer(int* x) { *x = 100; }
```

**函数模板:**
```cpp
template<typename T>
T add(T a, T b) {
    return a + b;
}

// 使用
add(1, 2);        // int
add(1.5, 2.5);    // double
```

### C++ 面向对象基础

**类的定义:**
```cpp
class Person {
private:
    std::string name;
    int age;
    
public:
    // 构造函数
    Person(std::string n, int a) : name(n), age(a) {}
    
    // 成员函数
    void introduce() const {
        std::cout << "Hi, I'm " << name << ", " << age << " years old.\n";
    }
    
    // Getter/Setter
    std::string getName() const { return name; }
    void setAge(int a) { age = a; }
    
    // 析构函数
    ~Person() {
        // 清理资源
    }
};
```

**继承:**
```cpp
class Student : public Person {
private:
    std::string studentId;
    
public:
    Student(std::string n, int a, std::string id) 
        : Person(n, a), studentId(id) {}
    
    void study() const {
        std::cout << name << " is studying.\n";
    }
};
```

**访问控制:**
- `public` - 公开访问
- `private` - 仅类内部访问
- `protected` - 类和子类可访问

---

## 💻 实践练习

**任务:** 实现一个简单的 `Rectangle` 类
- 属性：width, height
- 方法：计算面积、周长
- 构造函数、析构函数

---

## 💡 今日总结

**总学习时长:** _待填写_

**完成度:** ⭐⭐⭐⭐⭐ (1-5 星)

**关键收获:**
_学习中..._

**遇到的问题:**
_待记录..._

**明日计划:**
_待补充..._

---

_坚持就是胜利！🌰_
